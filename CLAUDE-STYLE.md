# Claude 风格改造说明

本分支（`claude-style`）把「问问大师」从**暗色玻璃拟态 / 霓虹光效**改造为
**Claude.ai 风格**：米白纸感底、赭橙单强调色、扁平卡片、宽松排版，并支持浅色 / 深色双主题。

---

## 一、设计对照

| 维度 | 改造前 | 改造后（Claude 风格） |
|---|---|---|
| 底色 | 纯黑 + 动态渐变网格 + 粒子星空 | 浅色 `#faf9f5` / 深色 `#262624`，无动态背景 |
| 强调色 | 随「用神」切换五套色相（木绿/火红/土黄/金灰/水蓝） | 统一 Claude 赭橙 `#d97757` |
| 卡片 | 半透明 + `backdrop-filter` 磨砂 + 大面积深投影 | 纯色表面 + 1px 描边 + `0 1px 2px` 极轻阴影 |
| 按钮 | 渐变玻璃胶囊 + 光晕 | 实心赭橙主按钮 / 描边幽灵次按钮 |
| 字体 | 全站无衬线，宽字距 | 标题衬线（Tiempos/宋体族）+ 正文无衬线，字距归零 |
| 光标 | 自定义跟随光球 + 光环 | 系统原生光标 |
| 底栏 | 液态玻璃 dock + 轨道动画 AI 球 | 纯色胶囊 + 柔和选中态 |
| 主题 | 仅暗色 | 浅色 / 深色 / 跟随系统，可一键切换 |

---

## 二、新增文件

| 文件 | 作用 |
|---|---|
| `src/theme-claude.css` | **设计令牌层**。定义 `--c-bg / --c-surface* / --c-text* / --c-border* / --c-accent*` 与五行色，浅深两套值 |
| `src/claude-ui.css` | **组件层**。把既有类名重塑为 Claude 外观；末尾重新声明 `--ac*` 等派生变量以覆盖 `styles.css` 的 `:root` |
| `src/ui/theme.js` | 主题切换逻辑：读写 `localStorage.tj_theme`、注入切换按钮、响应系统主题变化 |

样式加载顺序（`index.html`）：

```
theme-claude.css  →  styles.css  →  ig-dock.css  →  claude-ui.css
   令牌                原有样式        底栏样式         Claude 覆盖层
```

`index.html` `<head>` 内含一段内联脚本，在首屏渲染前写入 `data-theme`，避免明暗闪烁。

---

## 三、代码改动

### 语义化配色（codemod）
原代码在 CSS 与 JS 模板字符串里写死了上千处 `rgba(255,255,255,.xx)` / `#fff` / 深色 `rgba(0,0,0,.x)`。
`tools/` 下的脚本按 **CSS 属性上下文**（`color` / `background` / `border`）把它们批量映射到语义令牌：

| 脚本 | 作用 |
|---|---|
| `tools/claudeify.py` | 第一轮：白系颜色 → `--c-text*` / `--c-surface*` / `--c-border*` |
| `tools/claudeify2.py` | 第二轮：暗色底（低明度 rgba/hex/hsla）→ 同上 |
| `tools/strip-glass.py` | 删除 15 条 `[data-glass]`「玻璃质感强度」规则 |
| `tools/strip-shadows.py` | 中和 48 条深黑投影（在浅底上会变成脏灰光晕） |

### 手工改动
- `src/engines/shared.js` — 五行色 `WC` 改为 `var(--wx-*)`，随主题切换明度
- `src/engines/bazi.js` — 神煞标签配色改用 `color-mix()` + 语义色
- `src/ui/navigation.js` — `applyTheme()` 不再按用神改写全站色相，只写 `data-yongshen` 数据标记
- `src/render/report.js` — canvas 图表网格线/轴标签跟随明暗；剩余写死色值改为语义变量
- `src/main.js` — 图表强调色固定为赭橙；接入 `initTheme()`

---

## 四、主题切换

- 首页右上角 / 报告页顶栏各有一个 🌙☀️ 切换按钮
- 默认跟随系统；用户手动切换后固定，写入 `localStorage.tj_theme`
- 也可在控制台调用 `toggleTheme()`

---

## 五、验证

`tools/` 下的 Puppeteer 脚本用于回归检查：

```bash
npm run build
npx vite preview --port 4173 &

node tools/shot.mjs      # 首页/报告/运势/工具/AI 面板 × 明暗 → /tmp/shots
node tools/shot2.mjs     # 大师模式/工具弹层/保存弹窗 × 明暗 → /tmp/shots2
node tools/contrast.mjs  # WCAG AA 对比度审计
```

当前 `contrast.mjs` 结果：**浅色 0 处、深色 0 处**低对比文本（AA 标准，正文 4.5:1、大字 3:1）。

---

## 六、回滚

```bash
git checkout main          # 回到改造前
git diff main claude-style # 查看全部改动
```

---

## 七、细节精修（`src/claude-refine.css`）

首轮改造只统一了「配色」，第二轮针对**排版系统与交互尺寸**做了收口。

### 发现的问题（实测数据）

| 问题 | 改造前 | 改造后 |
|---|---|---|
| 字号种类 | 17 种，最小 **8.7px**（中文糊成一团） | 8 种，最小 **11px** |
| 圆角种类 | 9 种（6/8/12/15/16/17/50%/100/999px） | 4 种（12 / 16 / 999px / 50%） |
| 卡片内距 | 14~28px 混用 | 20 / 24px 两档 |
| 触控目标 | AI 面板按钮仅 **29×29** | 全部 ≥ 40×40 |

### 根因与解法

**字号失控**：旧设计用 `em` 层层相乘 —— `.qr-summary{.84em} > b{.74em}` 或
`.structure-mini-bd{.84em} > .fine-table{.7em}`，15px 基准经两三层就压到 8~9px。
解法是引入 **px 字阶**（`--fs-micro` 11px ~ `--fs-3xl` 30px），
并在中间层容器上重置 `font-size`，**阻断 em 相乘链**。

**触控目标**：`styles.css` 里有 `#aiSheet .ai-new{width:29px!important}` 这类
ID + `!important` 的窄屏规则，普通类选择器改不动，需同等特异性覆盖。

**中文排版**：原设计大量使用 `letter-spacing: 1.5px` 与 `text-transform: uppercase` ——
这两者都是为拉丁文设计的，中文加宽字距会破坏词组感，`uppercase` 更是无效。已统一归零。

### 顺带修复的 Bug
- `💾` emoji 在无 emoji 字体的环境下渲染为豆腐块 → 换成线性 SVG 图标
- 首页浮动主题按钮因通用 `display:inline-flex` 覆盖，在报告页泄漏出第二个月亮图标 → 补 `body:not(.home)` 规则

### 验证
```
node tools/contrast.mjs   # 明暗双主题均 0 处低对比
```

---

## 八、八字合盘（`src/engines/synastry.js`）

### 改造前的问题

排查时发现关系功能存在**三套并行实现**，且都不是真正的合盘：

| 位置 | 问题 |
|---|---|
| `tools/relation.js` | 收了对方生日**但从未使用**，只回显用户自己的 `d.ls` 分数 |
| `render/report.js` `calcRelation()` | 有排盘，但时辰缺失时**静默填 `06:00`**（凭空捏造时柱）；且输出「上婚/下婚」「白头偕老概率高」这类结论 |
| `main.js` v3 工具引擎 | 实际生效的那套，只有通用沟通话术，无任何命盘计算 |

更关键的是：`render/report.js` 的布局重排会把 `[data-card="relAi"]` **整个删掉**，所以那套实现用户根本看不到——这也是占位逻辑长期没被发现的原因。

### 现在的实现

新增独立引擎，三个入口统一调用：

- **真实排盘** —— 用 `mkBazi()` 为对方排盘。注意**不能调用 `mkDy()`**，它内部直接读页面上本人的 `bDate` 输入框。
- **逐柱比对** —— 天干五合/相冲、地支六合/六冲/相害/三合/相刑，按柱位加权（日柱为夫妻宫，权重 3.0）。
- **日主十神** —— 对方日主之于我是正印/七杀等，映射为「照顾支持」「推动压力」等关系角色。
- **五行互补** —— 对方命局能否补到我的用神。
- **去重** —— 月柱与日柱干支相同时会产生重复文案，展示层只保留权重最高的一条。

### 时辰缺失的处理

这是合盘最现实的约束：**多数人拿不到对方的出生时辰**。

处理方式是分级而非捏造：

| 精度 | 条件 | 说明 |
|---|---|---|
| `full` | 已知时辰 | 四柱全比对 |
| `day` | 仅知年月日 | 三柱比对，UI 明确标注 |

关键在于 **日柱不依赖时辰**（`getDayPillarIndex` 只用年月日），而日柱正是合婚中的夫妻宫——所以三柱模式下核心结论依然成立，缺失的时柱只影响子女宫与晚年节奏的判断。这一点在结果页直接向用户说明，不含糊过去。

### 文案取向

移除了「上婚/下婚」「白头偕老概率高」等结论式判断，改为描述差异与给出沟通方式，并统一加注：

> 合盘用于理解彼此差异、找到沟通方式，不预测关系结局，也不构成是否开始或结束一段关系的建议。

### 测试

```
node tools/test-synastry.mjs   # 15 项，含日柱锚点校验与 600 组随机样本边界检查
```

日柱正确性以公认锚点交叉验证：`2000-01-01 = 戊午`、`2024-01-01 = 甲子`。

---

## 九、合盘对象档案与分享

### 一处更正

上一轮我判断「命盘没有持久化」，**这个结论是错的**。当时只 grep 了 `localStorage`，
漏掉了 `main.js` 中已有的一整套 IndexedDB 实现（`initDB / dbPut / dbGetAll / dbDel`）。
实测确认：保存 → 刷新 → 重新载入全部正常，`loadProfile()` 也会自动触发 `calc()` 进入报告页。

因此本轮不重做地基，只补真正缺失的两块：**合盘对象无法保存**、**结果无法分享**。

### 新增

**`src/state/partners.js`** —— 合盘对象存储
- 用 `localStorage` 而非 IndexedDB：这是一份很轻的数据（姓名 + 生日 + 可选时辰），
  也避免与 profiles 的 schema 纠缠
- 同名 + 同生日视为同一人，更新而不是产生重复条目
- 上限 20 条，按最近使用排序

**`src/tools/synastry-share.js`** —— 分享与对象选择器
- 三个入口共用，避免再次出现「多套实现各自漂移」
- 表单顶部展示「最近合盘对象」，点击回填姓名/生日/时辰/关系类型
- 结果页底部提供「分享结果」「保存这个人」

### 分享的降级链

原本只做到「原生分享 → 剪贴板」，但测试中发现剪贴板 API 在权限受限时会直接抛错，
用户只会收到一句「当前环境不支持分享」，**文本就此丢失**。改为四级降级：

```
navigator.share  →  clipboard.writeText  →  execCommand('copy')  →  可选中文本弹层
```

其中 `AbortError`（用户主动取消原生分享）不再继续降级。
实测在剪贴板被拒的无头环境下，`execCommand` 成功兜住。

### 测试

```
node tools/test-partners.mjs   # 7 个场景：保存 / 分享 / 回填 / 刷新留存 / 删除
node tools/test-persist.mjs    # 命盘档案持久化回归
```

注：`loadProfile()` 会触发约 2 秒的推演动画，断言需等待其结束——
最初的测试因等待不足而误报「未跳转报告页」。

---

## 十、每日触点（`src/engines/liuri.js`）

### 修掉的一处死代码

旧「流日日签」里有这样一行：

```js
const relation = dayZhi === ((typeof __TJX_V5!=='undefined' && '') || '') ? '' : '流日地支「'+dayZhi+'」提示：安排留白…';
```

条件恒为 `false`，所以**地支比对从未真正发生**，每天都输出同一句通用提示。
这正是 Co-Star 被批评的模式：片段按天轮换，彼此不感知，也不与用户命盘产生关系。

### 新引擎的做法

先把「当日干支 × 本人四柱」的全部互动算出来（六合／相冲／相害／天干五合，
按柱位加权：日柱 3.0 > 月柱 1.8 > 时柱 1.4 > 年柱 1.0），
叠加「今日五行 与 用神」的生克关系，汇总成一个 `energy` 值与单一 `tone`，
**再由 tone 统一驱动全部文案**。

这样能避免竞品那种「周一让你谨慎、周二让你大胆」的自相矛盾——
同一天内所有建议来自同一个基调。

输出示例（真实运行结果）：

> 流日「申」与你时柱「巳」六合——晚上的时间比平时更容易配合。
> 有需要沟通或请求协助的事，今天开口成本较低。

这句话依赖用户的时柱，**无法套用到别人身上**。

### 刻意不做的事

- 不输出吉凶断言（测试用 10 个词做黑名单校验：大凶／必有／破财／血光／小人…）
- 不制造紧迫感来促使打开
- 同一天内容固定（`getDayPillarIndex` 是纯函数，同日必然同结果），
  不做「刷新就换一句」的伪随机

基调分布实测（400 天）：`steady 144 / flow 128 / friction 89 / rest 39`——
偏正向，不靠制造焦虑维持打开率。

### 首页「今日一句」

有档案时在**表单之前**展示当日内容，回访用户无需重新推演即可看到。
无档案时不显示，不打扰新用户。

### 顺带修掉的布局 Bug

`.p1-scroll` 用 `justify-content: center` 做垂直居中。内容超过一屏时，
flex 会把溢出部分推到 `scrollTop = 0` **之上**，导致「今日一句」滚动到顶也看不见
（实测 `scrollTop: 0` 但 `cardTop: -163`）。
改用 `margin: auto` 实现居中——内容不足时依然居中，超高时正常从顶部开始。

### 测试

```
node tools/test-liuri.mjs   # 12 项：干支锚点 / 同日稳定 / 因人而异 / 400 天自洽 / 无吉凶用词
node tools/test-daily.mjs   # 6 个 E2E 场景
```

---

## 十一、可读性改造（P0 + P1）

### 诊断依据

实测新手 / 大师两档的术语密度：

| 模式 | 正文 | 术语出现 | 密度 |
|---|---|---|---|
| 新手 | 341 字 | 2 次 | **0.6%** |
| 大师 | 493 字 | 45 次 | **9.1%** |

新手模式本身克制得不错（1 屏读完），真正的问题是**两档之间是悬崖不是台阶**：
点「查看完整专业依据」后密度跳 15 倍，且「格局／命局／喜神」等 13 个高频词
**完全没有解释入口**。

### P0：补齐术语，并让用户知道「能点」

1. **词库 34 → 55 条**，补全报告中出现却无解释的 13 个词，
   并把十神格局补成完整一套（食神格／伤官格／正官格／七杀格／正财格／
   偏财格／印绶格／比肩格／劫财格／建禄格），因为格局是新手最先撞上的词。

2. **可供性**：原设计只有 1px 虚线 + `cursor:help`——移动端没有 hover，
   用户根本不知道能点。现改为强调色 + 上标问号 + 点按反馈。

3. **一次性引导**：首次进报告显示「看到不懂的词？带虚线的术语都可以点开看白话解释」，
   关闭或点过任意术语后不再出现（`localStorage: tj_gloss_hint_seen`）。

实现上踩到一个坑：`calc()` 调用的是 **import 进来的** `showPage2`，
包装 `window.showPage2` 完全不生效；改为监听 `body.report-active` 类变化。

### P1：首屏从「数据」改为「数据 + 含义」

改造前四个格子是：`生肖 马 / 日主 辛 / 有利方向 水元素 / 当前大运 —`，
对小白而言是四个孤立符号。现在每格都带一句话：

| 改造前 | 改造后 |
|---|---|
| 日主　辛 | **我是什么样的人**　辛（金）<br>像珠玉，精致、对细节要求高 |
| 有利方向　水元素 | **对我有利的方向**　水元素<br>保持灵活，多观察再动 |
| 当前大运　— | **当前人生阶段**　壬辰<br>27–36岁这十年 |

### 顺带修掉的一个既有 Bug

「当前大运」格**恒为空**。原因是代码读的是 `ctx.dw`，
而 `dw` 在 context 里是**日主五行**（如「金」），当前大运其实在 `ctx.cDy`。
这个 bug 在改造前就存在，因为该格一直显示「—」而没被注意到。

### 测试

```
node tools/test-gloss.mjs        # 引导出现/消失/不重复 + 新词条可解释
node tools/test-firstscreen.mjs  # 首屏四格含义
```

---

## 十二、把「不会问」变成「点一下就问对」（P2）

### 起因：一次真实的答非所问

调研指出新手最大的障碍是「看不懂，又不知道怎么问」。
实测验证了这一点——向 AI 提问「食神格是什么意思？」，返回的却是：

> **我最近为什么压力大？** 近期官杀气重，压力指数偏高…

追查后发现三个叠加的 Bug：

1. **术语分支被长度门槛挡住**：`smartAnswer` 里写的是 `if(term && q.length<=10)`，
   而这句话有 11 个字，直接落到模糊 FAQ 匹配上。
2. **两份术语表已经漂移**：报告用的 `GLOSSARY`（55 条）与 AI 用的 `KB.terms`（23 条）
   各自维护，`食神格` 只存在于前者。
3. **FAQ 兜底门槛过低**：错配项以 8.4 分通过了 `sc>=8` 的判定。

### 修复

- `findTerm` 改为**取最长匹配**（否则「食神格」会被「食神」抢先命中），
  并把 `GLOSSARY` 注入检索池，两份词表统一。
- 术语分支门槛从「字数 ≤10」改为**看提问意图**（是什么／什么意思／怎么理解…）。
- FAQ 兜底门槛 `8 → 12`：宁可交给 AI，也不要给出无关答案。

### 新增：「这段是什么意思」

`src/ai/explain.js` 会把用户点击的那张卡片，自动组装成一个结构化提问：

> 「你有推进事情的魄力，也容易对自己要求很高」这段是什么意思？
> （原文：你通常愿意先站出来解决问题，适合负责需要推动、协调或决断的事…）

用户不需要自己想怎么问——这正是竞品指出的痛点。

实现中的坑：`querySelector('.bb-text, .at, p')` 是按**文档顺序**返回的，
不按选择器优先级，因此会先命中评分行等无关文本。改为逐个选择器依次尝试。

### 视觉克制

问号提示只加在**每张卡片第一个可见术语**上。
大师模式一屏有 58 个术语，全加问号会变成噪音；现在是 5 个。
判断可见性是必须的——新手/大师会隐藏大量卡片，
标到隐藏元素上会导致用户实际看到的那个反而没提示。

### 顺带修复

P1 改写首屏时移除了原有的可点术语，导致**新手模式一个可点术语都没有**
（58 个术语全在 `display:none` 的 `.qr-card` 里）。
现已把「日主／用神／大运」作为标签的一部分恢复为可点。

### 测试

```
node tools/test-explain.mjs   # 入口注入 / 自动提问 / 术语不再答非所问
```

---

## 十三、UI 缺陷修复（360 / 430 / 768 三档实测）

### 方法

新增 `tools/audit-layout2.mjs`，在三种视口 × 七个界面下自动检测：
横向溢出、文字裁切、触控目标过小、卡片重叠。
排除了两类误报：滑出视口的 `.page`（页面切换动画所需）与 sr-only 元素。

### 修复项

| # | 问题 | 实测数据 | 处理 |
|---|---|---|---|
| 1 | **工具中心 360px 塌成单列** | `grid-template-columns: 328px`，116px 高的卡只放两行短字，首屏仅 3 个工具 | 固定两列，卡高 116→86px，首屏 8 个 |
| 2 | **分类筛选条末项被切断** | `scrollWidth 374 > clientWidth 332`，无任何可滑动提示 | 右侧渐隐遮罩 + 滚动吸附 |
| 3 | **弹层关闭按钮压住简介卡** | close `303,93→345,135` 与 intro `23,109→337,245` 实测重叠 | 简介右侧留 52px；并移除背景图（导致文字对比度不稳） |
| 4 | **模式切换按钮高 30px** | 低于 WCAG 2.5.8 / iOS HIG | 统一 36px |
| 5 | **导出/导入按钮 48×29** | 内联样式写死，尺寸不足 | 抽为 `.pf-io-btn`，36px |
| 6 | **工具卡内部空隙过大** | 隐藏的 `.tool-icon` 仍带 `margin-bottom:12px` | 归零 |

### 一个值得记的坑

`.mode-top-switch` 只加 `min-height` 无效——`styles.css` 里有
`max-height:30px !important` 钳住高度，必须 `height/min-height/max-height`
三者同时覆盖。这类「同一属性被多层 `!important` 反复覆写」在本项目里很常见，
改样式前建议先用 CDP `getMatchedStylesForNode` 查清胜出规则。

### 回归

单测 15/15、E2E 6 套件全过、明暗对比度各 0 处不达标。

---

## 十四、两处体验修复

### 14.1 「这段是什么意思」没有带上下文

**复现**：点「四柱八字」的解释入口 → 提问「四柱八字这部分是什么意思？」
→ 返回**词典释义**「八字：把出生的年、月、日、时换算成天干地支…」。
用户要的是解读*他自己的*命盘，拿到的却是名词解释。

三个叠加原因：

1. `chartFacts()` 写好了但**从未被调用**——`main.js` 里 import 了却没用到，
   问题里因此不含任何命盘数据。
2. **卡片 key 对不上**：`CARD_TOPIC` 定义的是 `bazi/wuxing/timeline…`，
   实际 DOM 里是 `qr-card/beginner-brief/structure/three-styles/reasoning`，
   大部分卡片匹配不到模板，退化成通用句式。
3. **被术语词典拦截**：`smartAnswer` 只要问句含「什么意思」就走词典分支。

修复后，每张卡片按自己的模板拼入真实命盘：

> 我的四柱是 庚午 己丑 辛亥 癸巳，日主辛金。这组八字说明我是什么样的人？
> 请用大白话讲，不要只解释「八字」这个词的定义。

同时收紧词典分支：仅「短问句」走词典，含「我的/我是/请解释」的长提问交给 AI。

### 14.2 合盘结果卡过长

**实测**：9 个小节平铺 **810px 正文**（整卡 1170px），需滚动 1.4 屏。

改为「结论常驻 + 四组折叠」：

| 分组 | 默认 | 说明 |
|---|---|---|
| 判语 + 一句话总述 | 常驻 | 不展开也知道结论 |
| 双方命盘 | 展开 | 理解后续的基础 |
| 关键结论 | 展开 | 夫妻宫 / 五行互补 / 日主关系 |
| 逐项依据 | 收起 | 最长的一块 |
| 沟通脚本 | 收起 | 需要时才看 |

正文 **810 → 702px**，默认一屏读完。

**踩到的坑**：`grid-template-rows: 0fr` 折叠动画只压第一行，
而「逐项依据」有 2 个子元素 → grid 自动创建第二行，**收不起来**
（实测收起态仍有 101px）。必须在 body 内再包一层 `.syn-group-inner`。

另外发现三层标题重复显示同一个分数（页头 / result 标题条 / 我加的 verdict），
已去掉重复项。

### 测试

```
node tools/test-explain-ctx.mjs   # 7 张卡片的提问是否都带命盘数据、且不返回词典
node tools/test-syn-fold.mjs      # 折叠结构 / 展开收起 / 高度收敛 / 触控 ≥44px
```

---

## 十五、细节打磨（逐项实测复现）

### 15.1 问问大师发送按钮"在框外"
实测 `.ai-input-row` 的 `padding: 0`，按钮右缘距边框仅 **1px**，视觉上贴在描边上；
`.ai-input` 又自带灰底，形成"框中框"。
修复：容器补 6px 内距 + `overflow:hidden`，输入框底色透明。
注意 `#aiSheet .ai-input:focus` 另有一条 `!important` 会在聚焦时把灰底加回来，需一并覆盖。

### 15.2 工具页顶部方形外框
`.tools-toolbar` 是纯白实心块（`bg: #fff`），与下方米色背景形成一条突兀的方形边界。
改为透明 + 仅保留一条下边框作分隔。

### 15.3 梅花易数显示不完整
**真实原因不是文字缺失，而是卦象根本没画出来。**
`_hexLines()` 生成的爻线 `<span>` 没有宽度，父级是 flex 行 → 实测塌成 **0×8px**；
容器又限死 `max-width:180px`。所以用户只看到一个孤零零的「动」字。
修复：爻线加 `flex:1 1 auto`，容器放宽到 200px，整体垂直居中。
爻线宽度 **0 → 151px**，六爻卦形（地泽临）现在完整可读。

### 15.4 当年运势数值不易滑动
`.y-hero` 是 `display:flex` 且**没有 `overflow-x`**，四项实测 424 > 348 溢出，
「健康」被切掉且**无法滚动**。
修复：改为 2×2 网格（≥560px 恢复四列），不再依赖横滑。

### 15.5 自查发现并修复
- **「离线」不解释含义**：用户不知道还能不能用。改为「离线模式 + 仍可查术语与命盘解读，接入 AI 后回答更贴合你的提问」。
- **折叠卡缺 `aria-expanded`**：读屏用户无法感知内容是收起的。已在注入与切换时同步。
- **命名不一致**：工具磁贴仍叫「AI 关系分析」，而功能已是真实合盘。统一为「八字合盘」。

### 回归
单测 15/15、解释上下文 7/7、合盘折叠 10/10、E2E 6 套件、
明暗对比度各 0 处、结构审计 0 问题。

---

## 十六、工具卡背景改用 SVG

### 动机与实测

原本 `public/` 下有 14 张 900×502 的 WEBP，共 **208KB**；
进入工具页会实际下载其中 **141.4KB**。

但这些图是给旧的暗色主题画的（深蓝底 + 金色线条），
在 Claude 浅色主题下并不搭 —— 之前的改造甚至给弹层加了
`background-image: none` 把它盖掉，**图仍然照常下载，纯属浪费**。

### 做法

`tools/gen-tool-art.mjs` 用几何基元生成 15 个 SVG：

| 基元 | 语义 | 用于 |
|---|---|---|
| `wave()` | 增长 / 节奏 | 财运、分组底纹 |
| `bars()` | 评估 / 指数 | 事业、裁员 |
| `rings()` | 循环 | 日签、穿搭 |
| `dots()` | 选择 / 随机 | 起名、选号 |
| `hexLines()` | 卜卦 | 摇签 |
| `venn()` | 关系 | 合盘、生肖 |

两处体积优化：
- 波形步长 8→20 并改二次贝塞尔，点数减半（wealth 5.1→3.3KB）
- 点阵改用 `<pattern>` 平铺，不再逐个输出 circle（name 5.9→0.5KB）

### 结果

| | 改造前 | 改造后 |
|---|---|---|
| 仓库资源 | 208KB / 14 文件 | **16.7KB** / 15 文件 |
| 工具页实际下载 | 141.4KB | **11.7KB（−92%）** |

### 两个踩到的坑

1. **`currentColor` 在 CSS `url()` 背景里无效**。
   外部 SVG 是独立文档，拿不到页面颜色。最初想靠 `currentColor` 自动适配明暗，
   实测完全不生效。改为在 SVG 内写死品牌赭色 `#d97757`，
   再由 CSS 的 `--art-op` 按主题调透明度（浅色 .14 / 深色 .20）。

2. **不透明渐变把底图整个盖住**。
   原 CSS 是 `linear-gradient(145deg, var(--c-surface-3), var(--c-surface)), url(...)`，
   第一层完全不透明，SVG 永远看不见。已改为透明渐变，
   并用 `::after` 承载纹理，保证文字始终在 `z-index:1` 之上。

### 回归
单测 15/15、解释 7/7、合盘 10/10、E2E 6 套件、明暗对比度各 0 处。

---

## 十七、六项细节打磨

### 17.1 全局去除 emoji
扫描出 60 余种非 ASCII 图形字符，区分处理：
- **删除**：🟢🔴✅❌📖💡🔑🧭💼📜🗓⚡⚠ 等彩色 emoji（跨平台字形不一致，与线性风格冲突）
- **换成汉字**：工具图标 ◉↗◇✦☼◎♧♡ → 财业择装签号肖合；大运主题 ◆☗⚔✶✿⚭ → 比官杀枭食
- **换成 SVG**：设置齿轮 ⚙ → 线性图标
- **保留**：☰☱☲☳☴☵☶☷（卦象是内容本身）、★☆（评分）、→←（排版箭头）

顺带删除了 `#lgFab`（早已隐藏的「玻璃质感」入口）这个死节点。

### 17.2–17.4 问问大师面板
实测三处问题并修复：

| 项 | 实测 | 修复后 |
|---|---|---|
| 桌面端过窄 | 固定 430px、56% 屏高 | 768px 起 720px 宽、1200px 起 **860px / 82% 高** |
| 底部非圆角 | `20px 20px 0 0`，但面板悬空 87~92px | 四角统一 `20px`，贴近底部 10~24px |
| 输入框与按钮不等高 | 44px vs 38px（差 6px） | 均 **40px**，`align-items: center` |

### 17.5 分区标签页（信息架构）
实测「关系」需滚 **3.2 屏**、「命盘」2.5 屏，用户要不断下滑才能找到内容。
改为**速读常驻 + 其余卡片标签化**：

| 分区 | 改造前 | 改造后 |
|---|---|---|
| 关系 | 2797px（3.2 屏） | **1256px（1.4 屏）** |
| 运势 | 1534px（1.8 屏） | **1120px（1.3 屏）** |
| 命盘 | 2143px（2.5 屏） | 1831px（2.1 屏）※ |

※ 命盘首个标签「命盘结构」本身 1026px，属内容固有高度。

**调试过程值得记录**——前后试了四种触发方式，三种被实测证伪：
1. 包装 `window.switchTab`：main.js 之后会重新赋值，包装失效
2. 初始化时观察 `.sec`：报告未渲染时 `.sec` 数量为 0，观察不到
3. 纯 MutationObserver 防抖：自身 DOM 写入不断重置计时器，永不执行
4. ✅ 轮询 + 稳定即停（连续三次签名一致后停止）

最后还有一个隐蔽 bug：`build()` 统计卡片时只看 `.sec` 的直接子元素，
而卡片已被移入面板 → 每轮都误判「没有卡片」→ 拆了又建，
实测每 400ms 循环一次。修复是把面板内的卡片一并计入。

### 17.6 亮色模式工具卡字体阴影过重
`styles.css` 有 `text-shadow: 0 1px 8px rgba(0,0,0,.75)`，
是为「白字压深色 WEBP」设计的。底图已换成浅色 SVG，这层黑影在亮色下成了脏边。已移除。

### 回归
单测 15/15、分区标签 15/15、合盘折叠 10/10、E2E 6 套件、明暗对比度各 0 处、结构审计 0 问题。

---

## 十八、速读折叠 + 标签下划线化

### 18.1 速读卡可折叠
`_qrCard()` 的头部改为 `<button>`（可键盘聚焦、44px 触控），右侧加箭头指示状态；
折叠状态按分区分别记忆（`localStorage: tj_qr_collapsed_v1`）。

收起时一并隐藏卡片末尾注入的「这段是什么意思」，避免空卡片里孤零零留一个按钮。

### 18.2 分区标签改下划线式
去掉胶囊外框与底色，改为居中 + 选中项 2px 下划线，整条下方一条分隔线。
窄屏（≤420px）标签放不下时改为左对齐并可横滑。

### 又一次踩到 `grid-template-rows: 0fr`
`.qr-body` 下有 4 个直接子元素，grid 会自动创建 4 行，
`0fr` 只压第一行 —— 实测收起后仍有 **153px**。
必须包一层 `.qr-body-inner`。这已是本项目第二次遇到（合盘分组同样问题）。

### 环境问题排查记录
运行回归时 `test-syn-fold` 与 E2E 突然大面积超时。
先怀疑是本次改动引入的轮询，逐一排查后发现：
**沙箱里累积了 78 个未退出的 `vite preview` 进程**，端口一路顺延到 4198，
资源耗尽导致 `networkidle0` 永不触发。

验证方法：`git stash` 回到改动前，同样失败 → 确认与本次改动无关。
`pkill` 清理后全部恢复。

顺带修掉一个真实隐患：`section-tabs.js` 里
`setInterval(attachCardObs, 1000)` 永久轮询、从不停止。
改为「挂载成功即停，最多重试 40 次」。

### 回归
单测 15/15、速读+标签 13/13、分区标签 15/15、解释 4/4、
合盘折叠 10/10、E2E 6 套件、明暗对比度各 0 处、结构审计 0 问题。

---

## 19. 标签内容去卡片化 + 聊天面板让位 Dock

### 19.1 问题（实测复现，非推断）

**标签栏下方是「容器套同名容器」**
`s-rel` 关系分区，标签写着「亲密关系画像」，紧接着下面又是一张
`padding:24px / border:1px / radius:16px` 的白卡，
卡内标头再写一遍「亲密关系画像」+ 一个「亲」字图标 + 副标题。
同一句话在 40px 内出现两次，白卡边框还与页面底色几乎同色（`#fff` on `#faf9f5`），
只制造分隔线噪音，不提供信息。

命盘分区更严重，是**三层**：
标签 → `.glass.card-1` 外卡 → `.structure-subcard` 灰底方块。

**聊天面板压住 Dock**
430×932 实测：面板 `bottom = 922`，Dock `top = 858` —— 整整盖住 **64px**，
Dock 只剩一条边露在外面。用户想从聊天切到别的分区必须先关面板。
根因是 §19.2 为了修「底部圆角像被截断」写了
`bottom: max(10px, env(safe-area-inset-bottom))`，把面板按到了屏幕底部，
而 Dock 本身也是 `position:fixed` 浮在底部，两者直接重叠。

### 19.2 改法

**去壳（`.sec-plain`）**
`section-tabs.js` 归位卡片时打上 `.sec-plain`，CSS 清掉底色/描边/圆角/内边距；
`teardown` 时移除，不污染非标签页形态。
二级子卡（`.structure-subcard`）同样去壳。

**精简标头（`_slimHead`）**
- 标题与标签同源 → 删标头，副标题降级成一行 `.sec-pane-desc` 说明
- 副标题只是在罗列下面那排子标签（如「四柱、五行、细盘与十神关系」，
  下面正好就是这四个子标签）→ 连副标题一起删
- 标题与标签不同源（极少数）→ 保留原标头，避免丢信息

**标签文案改固定短标签表**
原来靠 `slice(0, 6)` 截断，产出过「2026年流」这种被切断的词。
改为 `LABELS` 映射兜底 + 去「20XX年」前缀 + 去括号补充。

| 分区 | 改前 | 改后 |
|---|---|---|
| 命盘 | 命盘结构 / 人格画像 / 三式合参 / 命局判读依据 | 命盘 / 人格 / 三式 / 判读依据 |
| 运势 | 当年运势 / 人生时间线 / 流日流时 / **2026年流** / 大运时间轴 | 当年运势 / 大运十程 / 当下关注 / 流月 / 大运轴 |
| 关系 | 亲密关系画像 / 朋友关系 / 亲人关系 / 八字合盘 | 亲密关系 / 朋友 / 亲人 / 八字合盘 |

**聊天面板上移**
```css
bottom: calc(var(--ig-dock-height) + safe-area + 12px + 10px);
max-height: calc(100dvh - 上述 - 16px);
```
桌面端从写死 `height: 78vh` 改为 `min-height + max-height`，
高度自适应，永远不会算进 Dock 占的那段。
同时把 Dock 的 `z-index` 从 90 提到 210（遮罩 89、面板 301 之间），
保证 Dock 浮在遮罩之上，**可见且可点**。

### 19.3 又一次被优先级坑到

`.sec-pane > .sec-plain .structure-subcard { background: transparent !important }`
写完不生效，实测子卡仍是灰底。
用 CDP `CSS.getMatchedStylesForNode` 逐条打印匹配规则，发现
`claude-ui.css` 有 `#page2 .glass .glass { background: var(--c-surface-2) !important }`
—— **带 id 选择器，优先级更高**，`!important` 相同优先级下比不过。
补一条同样带 `#page2` 的规则才压住。

教训：`!important` 不是终点，同为 `!important` 时仍按选择器权重比较。

### 19.4 「这段是什么意思」重复

去壳后，外层卡与内部子卡各挂一个按钮，视觉上是两个紧挨着的同名按钮。
第一次修在 `section-tabs.js` 建标签时清理 —— **无效**：
`main.js` 的 `inject()` 由 MutationObserver 驱动，会在之后重新加回来。
正确位置是 `inject()` 自身末尾，注入完再统一去重。

### 19.5 回归

新增 `tools/test-flat-tabs.mjs`（33 项，已接入 `npm run test:e2e`）：
三个分区各验 7 项（无底色/无描边/无内边距/标头不重复/子卡去壳/按钮不重复/文案不截断），
四档宽度各验 3 项（不压 Dock / Dock 可点 / 面板高度可用）。

| 宽度 | 面板底 → Dock 顶间距 | 面板高 | Dock 可点 |
|---|---|---|---|
| 360 | 14px | 535px | ✓ |
| 430 | 14px（改前 −64px）| 512px | ✓ |
| 768 | 10px | 620px | ✓ |
| 1440 | 10px | 680px | ✓ |

其余：单测 15/15、速读+标签 13/13、分区标签 15/15、合盘折叠 10/10、
解释上下文 3/3、E2E 全通过、明暗对比度各 0 处低对比。

### 19.6 回归修复：新手版冒出大师版的标签栏

**用户反馈**：新手版下方不应显示大师版的标签切换。

**根因**：`styles.css` 里隐藏大师专属卡片的规则是
```css
body.beginner-mode .sec > .glass { display: none }
```
它依赖「卡片是 `.sec` 的**直接子元素**」这一契约。
§19.2 的标签页把卡片搬进了
`.sec > .sec-tabs-wrap > .sec-panes > .sec-pane`，**层级变深，选择器不再匹配** ——
于是新手模式下大师卡片全部照常显示，标签栏也照常出现。

**我漏掉的信号**：上一轮探测数据里，新手与大师两种模式的 panes 数量
完全一致（4/5/4）。当时看到了却没追问，属于疏忽。

**修法**：恢复契约，而不是再加一条 CSS 去压。
1. `build()` 开头判断 `beginner-mode` → 直接 `teardown(sec)`，
   卡片回到 `.sec` 直接子层级，原规则重新生效
2. `_slimHead` 由**删除**改为**隐藏**（`.sec-head-hidden` + 插入 `.sec-pane-desc`），
   并新增 `_restoreHead()`；`teardown` 时一并撤销 ——
   删掉的标头无法还原，这是原实现的隐藏缺陷
3. body class 的 MutationObserver 增加「模式变化立即 `run()`」，
   仅靠 400ms 轮询会有可见闪烁

**验证**：三轮 新手↔大师 来回切换 + 分区来回切，
新手 panes 恒为 0、大师恒为 4/5/4；
切回新手后 `sec-plain / sec-head-hidden / sec-pane-desc` 残留均为 **0**，完全还原。

**一个断言问题**：改成隐藏后，原「标头未与标签重复」断言开始报错。
先查是不是真 bug —— 实测标头 `display:none`、`offsetParent:null`、高度 0，
对用户完全不可见，是**断言只查 DOM 存在、没查可见性**写得不对，遂修断言。

测试扩充到 43 项，新增「新手/大师模式隔离」两轮共 10 项。
